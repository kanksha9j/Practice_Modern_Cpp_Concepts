Edge cases:
1. Single element : 0 or negative

   What the problem asks                             Pattern to reach for
1. Process digits of a number,Digit extraction       % 10 + / 10 loop
2. Find pair that sums to X /Find Complement         Hash map
3. Longest/shortest subarray                         Sliding window
4. Search in sorted array                            Binary search
5. Check brackets/order valid                        Stack
6. Traverse tree/graph                               BFS queue or DFS recursion
7. Count occurrences                                 Frequency map, Hash map/set
8. Reverse / detect cycle in list                    Two pointers (fast/slow)

The decision rule — ask one question to select container
"What do I need to store, and what do I need to look up?"

Your answer tells you the container directly:
Store value only, look up "does X exist?"     →  unordered_set
check uniqueness of frequencies               =  unoredered_set
Store value→index, look up "where is X?"      →  unordered_map
Store value→count, look up "how many X?"      →  unordered_map
Need sorted order                             →  map or set (ordered)
Need sequence, access by position             →  vector
Need fast front+back insert/delete            →  deque
LIFO (last in first out)                      →  stack
FIFO (first in first out)                     →  queue

Day 1-2:  3 problems using hash map
Day 3:    3 problems using sliding window  
Day 4:    3 problems using stack + binary search
Day 5:    Mix — identify pattern first, then solve


Leetcode Solved problems: Pattern Card

Problem Type     Pattern                      Keyline
Palindrome       process the digit            access every digit, create reverse number, compare.
Two Sum          Find pair that sums to x    | WHY: need O(1) lookup of complement
                                             | KEY LINE: complement = target - nums[i]
                                             |           if seen.count(complement) → found
                                             |           else seen[nums[i]] = i   
Longest prefix   Prefix reduction            Take whole string as prefix , then reduce the prefix after comparing 
string                                       with all strings accordingly. This is not sliding window as multiple strings are involved.

3 LONGEST COMMON PREFIX
Pattern  : Prefix reduction
Why      : Start with full first string, shrink until it fits all
Key line : while str.find(prefix) != 0 → prefix = prefix.substr(0, size-1)
Edge     : Empty array → ""

#4 MOVE ZEROES
Pattern  : Two pointers (slow/fast)
Why      : slow = where next non-zero goes, fast = scanner
Key line : if nums[fast] != 0 → swap(slow,fast) → slow++
           fast++ always (for loop)

5. Count unique occurrences : 
Pattern: Frequency map, hash map/set  
Why    : 1. Hash Map used to count frequency 
         2. hash set to check uniqueness of frequency.
KeyLine: mp[i]++; for every value in array.
         if st.count(freq) → return false
            else st.insert(freq)
        

Learnings:
1. Two Sum:
- we use unordered container for two sum because its good for lookup.
- unordered containers dont use distance because they dont have meaningful indices.
- unordered_multiset - even if duplicates are allowed here, you cant access indices of these duplicates.
- So use unordered_map- doesnt take duplicates , but the keys are unique , so you know the proper index to access
- reserve space for unordered map.
- instead of insert pair(slower) use access specifier mp[nums[i]=i; 
- insert value as key because map searches with key.
